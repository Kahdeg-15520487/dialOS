// Test script to demonstrate runtime error with source context
var x: 5;
var y: 10; 
var z: x + y;

os.console.log("Starting calculation...");

// This will cause a division by zero error
var divisor: 0;
var result: 100 / divisor;  // Line that will cause error

os.console.log("This should not print");